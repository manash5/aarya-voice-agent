# Aarya voice agents (LiveKit)

Thin entrypoints at the repo root of this folder; shared code under `aarya/`.

```bash
uv sync
cp .env.example .env.local   # fill API keys
uv run agent.py console
```

See the root [README](../README.md) for the full layout, agent table, and how to
add a company profile or pipeline.
